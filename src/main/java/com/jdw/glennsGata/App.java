package com.jdw.glennsGata;

import java.util.Queue;
import java.util.Set;

/**
 * Hello world!
 *
 */
public class App {/*
	private static boolean solve(Board b) {
		Set<Integer> positions = b.getRemainigPositions();
		Set<Character> digits = b.getRemainigDigits(); 
		
		if (positions.size() > 1) {
			Integer pos = (Integer) positions.toArray()[0];
			Character digit = (Character) digits.toArray()[0];
			
			b.setDigitAt(pos, digit);
			
			return solve(b);
		}
		else if (positions.size() == 1) {
			Integer pos = (Integer) positions.toArray()[0];
			Character digit = (Character) digits.toArray()[0];
			
			return solve(b);
		}
		
		return b.testBoard();
	}
	
	
	public static void solve() {
		Board b = new Board();
		
		solve(b);
		
		System.out.println(b);
	}
	*/
	
    public static void main( String[] args )
    {
    	//solve();
        
    }
}
